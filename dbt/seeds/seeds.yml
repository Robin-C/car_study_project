version: 2

seeds:
  - name: mapping_colors
    config:
      schema: source
  - name: mapping_models
    config:
      schema: source      
  - name: mapping_regions
    config:
      schema: source
  - name: models
    config:
      schema: source  
  - name: mapping_years
    config:
      schema: source                
  - name: mapping_km
    config:
      schema: source    
  - name: mapping_trim
    config:
      schema: source   

      